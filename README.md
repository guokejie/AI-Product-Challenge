# Vibe Coding Showcase

一个纯静态的项目展示页，用来记录已经开发过的 vibe coding 产品。

## 结构

- `index.html`: 页面结构
- `styles.css`: 页面样式
- `app.js`: 项目数据与卡片渲染逻辑

## 使用

直接在浏览器打开 `index.html` 即可预览。

如果想本地起一个简单静态服务，可以在当前目录执行：

```bash
python3 -m http.server 8080
```

然后访问 `http://localhost:8080/vibe-coding-showcase/`。
